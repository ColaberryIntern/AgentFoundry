apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-monitor-service
  labels:
    app.kubernetes.io/name: compliance-monitor-service
    app.kubernetes.io/part-of: agent-foundry
    app.kubernetes.io/component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: compliance-monitor-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: compliance-monitor-service
        app.kubernetes.io/part-of: agent-foundry
        app.kubernetes.io/component: service
    spec:
      containers:
        - name: compliance-monitor-service
          image: ghcr.io/OWNER/agent-foundry/compliance-monitor-service:latest
          ports:
            - containerPort: 3002
              name: http
          envFrom:
            - configMapRef:
                name: agent-foundry-config
            - secretRef:
                name: agent-foundry-secrets
          env:
            - name: PORT
              value: "3002"
            - name: SERVICE_NAME
              value: compliance-monitor-service
          resources:
            requests:
              cpu: 125m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          startupProbe:
            httpGet:
              path: /health
              port: 3002
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: compliance-monitor-service
  labels:
    app.kubernetes.io/name: compliance-monitor-service
    app.kubernetes.io/part-of: agent-foundry
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: compliance-monitor-service
